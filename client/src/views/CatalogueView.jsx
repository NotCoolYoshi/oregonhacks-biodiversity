export default function CatalogueView() {
  // TODO: list the user's caught species (from GET /api/catches or a user-scoped
  // variant), plus the "catchable nearby" list from
  // GET /api/region/:placeId/nearby to show un-caught silhouettes.
  // Phenology (GET /api/species/:taxonId/phenology) drives "in season" badges.
  return (
    <section>
      <h2>Catalogue</h2>
    </section>
  )
}
