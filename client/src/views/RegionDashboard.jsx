export default function RegionDashboard() {
  // TODO: GET /api/region/:placeId/score and render the health score,
  // its native/invasive breakdown, and a recent-activity feed.
  return (
    <section>
      <h2>Region Dashboard</h2>
    </section>
  )
}
